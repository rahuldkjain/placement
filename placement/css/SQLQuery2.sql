create database registration;
create table student(
student varchar(500),
roll varchar(500),
email varchar(500),
phone int,
program varchar(500),
yearofstudy int,
spassword varchar(500)
);
select * from student;
use database registration;
use registration;
create table studentdata(
student varchar(500),
roll varchar(500),
email varchar(500),
phone int,
program varchar(500),
yearofstudy int,
spassword varchar(500)
);